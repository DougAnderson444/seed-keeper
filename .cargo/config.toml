[env]
BASE_PATH = "/seed-keeper/" # Github pages base path for leptos-site (doesn't seem to work for child config files?)
RUST_BACKTRACE = "1"
RUST_LOG="debug"

[build]
rustflags = ["-A", "dead_code"]
